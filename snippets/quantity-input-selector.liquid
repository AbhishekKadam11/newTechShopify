{% comment %}
  Quantity input Selector

  Accepts:
  - min: {Number} Min (optional)
  - mix: {Number} Mix (optional)

  Usage:
  {% render 'quantity-input-selector' variant: variant %}
{% endcomment %}

<add-to-cart :min="min" :max="max"></add-to-cart>